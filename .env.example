# GWO-AMD Environment Configuration
# Copy this file to .env and adjust paths for your system

# Base data directory
# This should point to the directory containing met/JMA_DataBase
DATA_DIR=/mnt/d/Data

# JMA Database base path (will be constructed as ${DATA_DIR}/met/JMA_DataBase)
# Or override with absolute path:
# JMA_DATABASE_DIR=/mnt/d/Data/met/JMA_DataBase

# Specific database paths (optional overrides)
# GWO_HOURLY_DIR=${JMA_DATABASE_DIR}/GWO/Hourly
# GWO_DAILY_DIR=${JMA_DATABASE_DIR}/GWO/Daily
# AMD_DIR=${JMA_DATABASE_DIR}/AMD

# JMA Web Downloader output directory
JMA_DOWNLOAD_DIR=./jma_data
